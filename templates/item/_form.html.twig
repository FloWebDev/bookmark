{{ form_start(form, {'attr': {'novalidate': 'novalidate', 'style': 'font-size: 1rem;'}}) }}
    {{ include('item/_delete_form.html.twig') }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-dark" value="{{ Constant.SUBMIT_ACTION }}">
{{ form_end(form) }}
